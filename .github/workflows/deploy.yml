name: My First Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Runner Started Successfully"